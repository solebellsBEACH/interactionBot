{
  "name": "interaction-bot",
  "version": "1.0.0",
  "description": "Script Playwright que abre o LinkedIn com interface gráfica.",
  "main": "src/open-linkedin.ts",
  "scripts": {
    "start": "ts-node src/open-linkedin.ts",
    "open:linkedin": "ts-node src/open-linkedin.ts",
    "install:browsers": "npx playwright install chromium",
    "test": "playwright test",
    "test:ui": "playwright test --ui --headed"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "playwright": "^1.48.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
